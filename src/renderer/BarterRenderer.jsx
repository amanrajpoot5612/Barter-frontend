import React from 'react'

const BarterRenderer = () => {
    /* renders single barter element */
    // (section renderer) main component for rendering barter related UI and other components
    // barter has multiple sections
    // in barter their are sub component with title and carousel
    // sub components have their own renderer
  return (
    <div>BarterRenderer</div>
  )
}

export default BarterRenderer